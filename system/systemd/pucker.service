[Unit]
Description = Headless PTT for Bluetooth devices
Requires = systemd-user-sessions.service network.target sound.target bluetooth.target
After = multi-user.target

[Service]
User = pi
Group = pi
Type = simple
ExecStart =
Restart = always
RestartSec = 5

[Install]
WantedBy = multi-user.target
